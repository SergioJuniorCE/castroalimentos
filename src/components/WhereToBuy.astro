---
const stores = [
  {
    name: 'Soriana',
    color: '#e31837',
    bgColor: '#fef2f4',
    description: 'Encuéntranos en todas las tiendas Soriana de la región.',
  },
  {
    name: 'Ley',
    color: '#1a1a1a',
    bgColor: '#f5f5f5',
    description: 'Disponibles en el área de carnes frías y embutidos.',
  },
  {
    name: 'Super Norte',
    color: '#e8b000',
    bgColor: '#fefce8',
    description: 'En la sección de productos regionales y artesanales.',
  },
  {
    name: 'Calimax',
    color: '#005baa',
    bgColor: '#eff6ff',
    description: 'Presentes en todas las sucursales Calimax.',
  },
];
---

<section id="donde-comprar" class="py-20 lg:py-28 bg-[#FFFDF9]">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Section header -->
    <div class="text-center mb-14">
      <span class="inline-block text-sm font-semibold uppercase tracking-widest text-[#A0522D] mb-3">Distribución</span>
      <h2 class="text-3xl sm:text-4xl lg:text-5xl font-bold text-[#3D1A0E]" >
        Dónde encontrarnos
      </h2>
      <p class="mt-4 text-lg text-[#5C2E1F]/70 max-w-2xl mx-auto">
        Nuestros productos están disponibles en las principales cadenas de autoservicio de la región.
      </p>
    </div>

    <!-- Promotional image -->
    <div class="rounded-2xl overflow-hidden shadow-lg mb-12 max-w-3xl mx-auto">
      <img
        src="/images/tamales-autoservicios.png"
        alt="Tamales Castro Alimentos disponibles en autoservicios"
        class="w-full h-auto object-cover"
      />
    </div>

    <!-- Store cards -->
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-5">
      {stores.map((store) => (
        <div
          class="group rounded-2xl border border-[#D4956A]/20 bg-white p-6 text-center shadow-sm hover:shadow-md hover:-translate-y-1 transition-all duration-300"
        >
          <!-- Store name styled -->
          <div
            class="w-full h-16 rounded-xl flex items-center justify-center mb-4 text-xl font-black tracking-tight"
            style={`background-color: ${store.bgColor}; color: ${store.color};`}
          >
            {store.name}
          </div>
          <p class="text-sm text-[#5C2E1F]/70 leading-relaxed">{store.description}</p>
        </div>
      ))}
    </div>

    <!-- Call to action note -->
    <div class="mt-10 text-center">
      <p class="text-[#5C2E1F]/60 text-sm">
        ¿No encuentras nuestros productos en tu tienda? <a href="#contacto" class="font-semibold text-[#7B3F2D] hover:underline">Contáctanos</a> y con gusto te orientamos.
      </p>
    </div>
  </div>
</section>
